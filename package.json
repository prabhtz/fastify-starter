{
  "name": "fastify-starter",
  "version": "1.0.0",
  "description": "Fastify starter pack",
  "main": "index.ts",
  "author": "prabhtz <gunpaul41@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "yarn clean && tsc",
    "start:dev": "yarn build && nodemon dist/index.js",
    "start": "build && node dist/index.js",
    "clean": "rm -rf ./dist"
  },
  "dependencies": {
    "fastify": "^3.26.0",
    "fastify-autoload": "^3.10.0",
    "fluent-json-schema": "^3.0.1"
  },
  "devDependencies": {
    "@types/node": "^17.0.9",
    "nodemon": "^2.0.15",
    "typescript": "^4.5.4"
  }
}
